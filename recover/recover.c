#include <stdio.h>
#include <stdlib.h>

typedef uint8_t BYTE;

int main(int argc, char *argv[])
{
    if (argc != 2) {
        return 1;
    }
    char *infile = argv[1];
    FILE *f = fopen(s, "r");
    if (f == NULL)
    {
        printf("Could not open %s.\n", infile);
        return 4;
    }
    while (fread())
    {

    }
}
