#include <stdio.h>
#include <cs50.h>

int main() {
    int height;
    do {
        height = get_int()
    } while(height < 1 && height > 8)
    for(int i = 0, i < height, i++){
        for(int j = 0, j < height, j++) {
            
        }
    }
    return 0;
}
